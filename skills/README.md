# Skills

Each folder in this directory is a standalone skill package.

Required:

- `SKILL.md`
- YAML front matter with `name` and quoted `description`

Optional:

- `agents/openai.yaml` for app/runtime display metadata
- `scripts/` for repeatable helper commands
- `references/` for longer supporting material
- `assets/` for static files used by the skill

